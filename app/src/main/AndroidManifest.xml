<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.ardy.jetpackardy">

    <application
        android:allowBackup="false"
        android:icon="@mipmap/logotonton_round"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Jetpackardy"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".tontonApp.DetailTonton"></activity>
        <activity
            android:name=".home.Splash"
            android:theme="@style/Theme.AppCompat.Light.NoActionBar">
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".detail.DetailTvActivity"
            android:label="@string/title_activity_detail_tv"
            android:parentActivityName=".home.MainActivity"
            android:theme="@style/Theme.Jetpackardy.NoActionBar" />
        <activity
            android:name=".detail.DetailMoviesActivity"
            android:label="@string/title_activity_detail_movie"
            android:parentActivityName=".home.MainActivity"
            android:theme="@style/Theme.Jetpackardy.NoActionBar" />
        <activity android:name=".home.MainActivity"></activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>